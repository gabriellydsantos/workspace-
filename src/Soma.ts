export class Soma {

    

    somar(numero1: number, number2: number): number {
        const total = numero1 + number2;
        return total;
    }

    multiplicar(numero1: number, number2: number): number {
        const total = numero1 * number2;
        return total;
    }
}

